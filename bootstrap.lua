function bootstrap()
    -- Put one-time setup here, for example dependency checks or marker files.
    -- Typical wrapper check:
    -- return reqpack.exec.run("command -v your-binary >/dev/null 2>&1").success
    return true
end
